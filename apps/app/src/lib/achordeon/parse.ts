/**
 * Parse Achordeon markup source into an intermediate representation.
 *
 * STUB: not implemented yet. Kept dependency-free (no Angular, no `vscode`)
 * so the Angular app and the future VSCode preview extension can share it.
 * Return type is `unknown` on purpose until the AST is designed.
 */
export function parse(source: string): unknown {
  // TODO: implement the real parser.
  return source;
}
