/**
 * Render a parsed Achordeon document into an HTML string.
 *
 * STUB: not implemented yet. Stays dependency-free so both the Angular app
 * and the future VSCode webview preview can call it. Takes `unknown` until
 * the AST produced by `parse` is designed.
 */
export function renderToHtml(ast: unknown): string {
  // TODO: implement the real renderer.
  return '';
}
