# shared-render-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-render-core` to build the library.

## Running unit tests

Run `nx test shared-render-core` to execute the unit tests via [Jest](https://jestjs.io).
